/// The app version. Kept in step with MARKETING_VERSION in the Xcode project,
/// the same rule the CLI's BuildInfo follows, and `make version-check` enforces
/// the match.
public enum AppVersion {
    public static let current = "1.2.0"
}
