## Why

The problem this change solves. Use real names: the screen, the file, the ssh option. Link the issue with `Fixes #123`.

## What

What the change does, in the same concrete terms.

## Changelog

The bullet you added under `## [Unreleased]` in `CHANGELOG.md`, or "No user-visible change".
