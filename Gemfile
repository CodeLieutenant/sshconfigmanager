source "https://rubygems.org"

# fastlane drives signing (match), building (gym), notarization, and the
# TestFlight/App Store uploads. Pinned via Gemfile.lock so CI and your laptop
# run identical versions — run `bundle install` once and commit the lockfile.
gem "fastlane"
